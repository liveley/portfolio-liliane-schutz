# Author: Liliane Schutz
# Backend Environment Variables Template

# Server Configuration
PORT=4000
NODE_ENV=development

# MongoDB Configuration (with Authentication)
# Replace <username> and <password> with actual credentials from .env.mongodb
MONGODB_URI=mongodb://<username>:<password>@localhost:27017/portfoliodb?authSource=portfoliodb

MONGO_APP_USER=portfolio_app
MONGO_APP_PASSWORD=your_password_here
MONGO_DB=portfoliodb

# CORS Configuration (Frontend Origins)
CORS_ORIGIN=http://localhost:3000
