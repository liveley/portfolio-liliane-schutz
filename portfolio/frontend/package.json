{
  "name": "portfolio",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev -p 3001",
    "dev:local": "next dev -p 3001",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "pages:dev": "start /B npm run dev:local && timeout /T 5 && wrangler pages dev --proxy=3001 --port=8788 --binding DB=portfoliodb",
    "pages:deploy": "npm run build && wrangler pages deploy .next --project-name=portfolio-website"
  },
  "dependencies": {
    "next": "16.1.1",
    "react": "19.2.3",
    "react-dom": "19.2.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260120.0",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.1.1",
    "typescript": "^5"
  }
}
